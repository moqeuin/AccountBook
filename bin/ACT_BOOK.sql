
CREATE TABLE ACT_BOOK(
	
	SEQ NUMBER(8),
	ID VARCHAR2(30),	-- 외래키
	IO_KIND VARCHAR2(1),	-- 수입/지출 구분
	AMOUNT NUMBER(9),	-- 금액
	CONTENT VARCHAR2(200),	-- 내용
	WDATE DATE	-- 입력일 SYSDATE
);

SELECT * FROM ACT_BOOK;

-- SEQUENCE

CREATE SEQUENCE SEQ
INCREMENT BY 1
START WITH 1

-- 외래키(ID)

ALTER TABLE ACT_BOOK
ADD
FOREIGN KEY(ID)
REFERENCES MEMBER(ID);

SELECT * FROM ACT_BOOK;
DROP TABLE ACT_BOOK;